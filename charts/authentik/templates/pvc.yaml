kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: "data-authentik-postgresql-longhorn"
spec:
  accessModes:
    - "ReadWriteOnce"
  storageClassName: "longhorn"
  resources:
    requests:
      storage: "10Gi"
